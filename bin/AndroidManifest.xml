<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xxyevolve.callingmusic"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />
    
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS"/>
	<uses-permission android:name="android.permission.READ_PHONE_STATE" />
	<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
	
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name="com.xxyevolve.callingmusic.dao.BelmotPlayer"
        >
        <activity android:name=".activity.LocalMusicListActivity" />
        <activity android:name=".activity.PlayerActivity" />
        <activity
            android:name="com.xxyevolve.callingmusic.activity.HomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <receiver android:name=".CallMusicReceiver">
　　　　		<intent-filter>    
                <action android:name="android.intent.action.PHONE_STATE"/>  
　　　   		</intent-filter>
　　		</receiver>

        <service android:enabled="true" android:name=".CallMusicService" /> 

    </application>

</manifest>
